Requirements: NodeJS  
Configuration:  
run cmd and goto project folder, and type the following:  
npm install  
bower install  
gulp compile-sass  
gulp copy-all  
---------------------------  
npm i -g gulp-cli  =>fixed if there is an error    
